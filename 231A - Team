#include <iostream>
#include <string>

using namespace std;

int main()
{
	int n;
	cin >> n;
	int solved = 0;

	for (int i = 0; i < n; i++) {
		int p, v, t;
		cin >> p >> v >> t;
		int sum = p + v + t;
		if (sum >= 2)
		{
			solved++;
		}
	}
	cout << solved << endl;
}
